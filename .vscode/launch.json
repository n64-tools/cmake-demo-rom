{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch debug ROM on ares Emulator",
            "type": "node",
            "request": "launch",
            "preLaunchTask": "ares-emulator", // The name of the task defined above
        },
        {
            "name": "Launch debug ROM on ED64 via USB",
            "type": "node",
            "request": "launch",
            "preLaunchTask": "ed64-usb", // The name of the task defined above
        }
    ]
}
