{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch debug ROM on ares Emulator",
            "type": "node",
            "request": "attach", // uses attach rather than launch so that it closes autmatically.
            "preLaunchTask": "ares-emulator", // Called in tasks.json
        },
        {
            "name": "Launch debug ROM on ED64 via USB",
            "type": "node",
            "request": "attach",
            "preLaunchTask": "ed64-usb", // Called in tasks.json
        }
    ]
}
